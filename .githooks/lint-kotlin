#!/bin/sh
#
# Formats Kotlin sources and Gradle scripts, and fails when detekt does. No --quiet: that hides the
# findings and leaves only "Analysis failed with N issues".

exec ./gradlew detektAll -Pdetekt.autoCorrect=true --console=plain
